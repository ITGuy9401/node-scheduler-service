Create a `json` file in the directory `../jobs-available`, 
then create a symlink inside this directory, pointing to that file, and with the file name ending 
with `.json`, for example `config.foo-bar.json`.
